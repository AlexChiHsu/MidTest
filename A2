const integer: number[] = [545, -845, 154, 0, -15]

let total = 0

for(let i = 0; i < 5; i++) {
    total += integer[i]       
}

let ave = total / 5.0 + ''
const rs = ave.indexOf('.')

if(ave.length - rs === 2) {
    ave += '0'
}
console.log("平均值: " + ave)
