const input = 16

const isPrimeNumber = (a: number): any => {
    for(let i = 2; i <= a - 1; i++) {
        if(a % i === 0) {
            return `不是質數`
        }
        else {
            return `是質數`
        }
    }
}

console.log(isPrimeNumber(input))
