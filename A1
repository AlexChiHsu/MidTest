const integer = [545, -845, 9523, 0, -15]

var max = integer[0]
var min = integer[0]

for(var i = 0; i < 5; i++) {
    
    if(max < integer[i]) {
        max = integer[i]
    }

    if(min >= integer[i]) {
        min = integer[i]
    }
    
}
console.log("最大值: " + max)
console.log("最小值: " + min)
