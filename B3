const input = 9
let count = 1
let str = ""
for(let i = 0; i <= input / 2; i++) {
    if(input % i === 0) {
        count++
        str += ("\n" + i + "*" + (input / i) + "\n")
    }
}
console.log(str + "\n" + "個數: " + count)
