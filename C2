const input = 1425

const arr = Array.from(input.toString())
let answer = 0

for(let i = 0; i < arr.length; i++) {
    answer += Number(arr[i]) * Math.pow(8, (arr.length - 1 - i))
}
console.log(answer)
